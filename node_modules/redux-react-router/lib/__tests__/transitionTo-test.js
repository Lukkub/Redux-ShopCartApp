'use strict';

var _ = require('../');

describe('transitionTo', function () {
  it('creates payload with pathname, query, state', function () {
    expect(_.transitionTo('/some/path', { foo: 'bar' }, { key: 123 }).payload).to.eql({
      pathname: '/some/path',
      query: {
        foo: 'bar'
      },
      state: {
        key: 123
      }
    });
    expect(_.transitionTo().payload).to.eql({
      pathname: undefined,
      query: undefined,
      state: undefined
    });
    expect(_.transitionTo(null).payload).to.eql({
      pathname: null,
      query: undefined,
      state: undefined
    });
    expect(_.transitionTo(false).payload).to.eql({
      pathname: false,
      query: undefined,
      state: undefined
    });
  });
});